<%= tag.li do %>
  <%= tag.a song.title, id: song.title, 
    class: 'song paper-btn btn-primary', 'data-action': 'click->music#play', 
    'data-href': ('songs/' + song.title) %> 
  <%= tag.a "~~~", href: "##{song.title}" %>
<% end %>
