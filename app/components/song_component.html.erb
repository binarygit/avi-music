<%= tag.li do %>
  <%= tag.a song.title, id: dom_id(song), 
    class: 'song paper-btn btn-primary', 'data-action': 'click->music#play', 
    'data-href': ('songs/' + song.title), 'data-id': song.id %> 
<% end %>
